# Screenshots

The main README embeds these three images. Drop PNGs here with these exact names:

- `fast-apply.png` — the Fast Apply queue (the home page)
- `discover.png` — the Discover / internships feed
- `packet.png` — an application packet

Suggested capture: run the app, resize to ~1200px wide, screenshot each page, and
save as PNG here. They'll appear in the README's Screenshots section automatically.
